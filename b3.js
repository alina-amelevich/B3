'use strict'
function calculationFunc(expression) {
    
}


function runB3() {
    let userExpression = prompt('Введите выражение, которое необходимо рассчитать');
    let calculatedAnswer = calculationFunc(userExpression);
    alert(calculatedAnswer);
}